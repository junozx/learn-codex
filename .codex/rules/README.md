# Rules Directory

This directory contains two different kinds of files:

1. `*.rules` files: loaded by Codex command-approval policy.
2. `python/*.md` and `typescript/*.md`: coding guidance docs referenced by `AGENTS.md`.

Only `*.rules` are parsed by the Rules engine automatically.

